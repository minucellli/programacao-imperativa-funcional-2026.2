o começo do código tem \n`. Em seguida, a sequência `\t` acrescenta uma tabulação antes da mensagem. Portanto, **“Bom dia! Shirley.”** será exibido em uma nova linha, com um espaçamento de tabulação antes do texto.

**b)** A mensagem **“Você já tomou café?”** será exibida normalmente e, ao final, ocorrerá uma quebra de linha. Isso acontece porque a sequência `\n` indica ao programa que o cursor deve passar para a próxima linha após a impressão da frase.

**c)** Nesse caso, são realizadas duas quebras de linha antes da mensagem **“A solução não existe!”**. Depois dessa frase, outra quebra de linha separa a mensagem **“Não insista.”**, que será apresentada na linha seguinte.

**d)** A instrução utiliza a sequência `\t` para inserir tabulações entre as palavras. Assim, **“Duas”, “linhas”, “de” e “saída”** aparecem separadas por espaços de tabulação. Em seguida, a sequência `\n` provoca uma quebra de linha, fazendo com que **“ou” e “uma?”** sejam exibidos na linha seguinte, também separados por uma tabulação.

**e)** A mensagem é formada por três palavras: **“um”, “dois” e “três”**. Entre elas são utilizadas quebras de linha, fazendo com que cada palavra seja apresentada em uma linha diferente. Portanto, a saída será composta pelas três palavras, uma abaixo da outra.

Essas respostas correspondem às cinco instruções de `printf()` apresentadas na Questão 07 da lista. 
